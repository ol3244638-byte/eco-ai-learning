# 学习记录

> **目标**：7 个月内完成从「环保」→「智慧环保产品 / 数据方向」的技能转型
> **开始日期**：2026-07-13
> **最后更新**：2026-07-29

---

## 📊 技能进度总览

| 技能 | 状态 | 备注 |
|------|------|------|
| Python 基础 | 🟡 进行中 | |
| Pandas | 🟡 进行中 | 2026-07-29 开始，已学：读取 / 查看 / 选列 / 筛选 |
| SQL | ⚪ 未开始 | |
| scikit-learn | ⚪ 未开始 | |
| LLM API 调用 | ⚪ 未开始 | |
| Gradio / Streamlit | ⚪ 未开始 | |
| Git / GitHub | 🟡 进行中 | |

> 图例：⚪ 未开始　🟡 进行中　🟢 已掌握

---

## 🚀 项目清单

| 项目 | 用到的技术 | 状态 | 链接 |
|------|-----------|------|------|
| A股预测器 | LSTM · akshare · pandas | 🟢 | [repo](#) |
| AQI分析简报工具 | Python · pandas · AI API | ⚪ | [repo](#) |

---

## 📅 每日记录

> **用法**：每天学完花 5 分钟写一条。点击日期标题即可展开 / 折叠当天内容（GitHub 和 VSCode 预览都支持），平时全部折叠，像翻笔记本一样一页页点开看。
> 最新的一天放最上面。每周日在「周小结」里回顾一次。

### 第 3 周（2026-07-27 ~ 08-02）—— Pandas 入门：读取和分析表格数据

<details>
<summary><b>📖 07-29 周四 ｜ 今日主题: pandas 排序与均值统计 —— sort_values() + mean()</b></summary>

**今天学了什么**

sort_values('列名', ascending=False) 降序排序;返回新表不改原表,结果要 print 或赋值才能看到
mean() 求均值,常与筛选连用:df[条件]['列'].mean()
多列选取要传列表:df[['date','PM2.5','AQI']],外层是取数语法、内层是列名清单
核心流水线:筛选 → 选列/排序 → 统计/取前N,三问共用一个套路

实战练习: 完成"福州PM2.5超标统计"一题(限值按 GB 3095—2026 日均二级 50 μg/m³)

超标天数:布尔筛选 + .shape[0]
超标日 AQI 均值 vs 全部日均值:筛选后 .mean() 对比,验证了超标日污染显著更高
污染最重前3天:sort_values 降序 + head(3),6月8日 PM2.5=72 为最重日

踩坑记录(括号三兄弟):

[] 筛选行、选列:df[...]
() 调用函数、包条件:print()、(df['pm25']>50)
{} f-string 嵌变量:f'{count_all:.1f}'
另:列名严格区分大小写,写代码前先 print(list(df.columns)) 确认

</details>

<details>
<summary><b>📖 07-29 周三 ｜ pandas 读取、查看、选列、条件筛选</b></summary>

**今天学了什么**
- `pd.read_csv()` 读取 CSV → DataFrame；`parse_dates=["date"]` 把日期列读成 datetime
- 查看三件套：`df.head()` / `df.shape` / `df.info()`
- 选列：`df["aqi"]` 单列（Series）、`df[["date","pm25"]]` 多列（注意双层方括号）
- 条件筛选：`df[df["aqi"] > 100]` 筛超标日；`&`（且）`|`（或）组合条件，每个条件要加圆括号
- `.dt.day_name()` 从日期抽星期几；`.isin([...])` 判断值是否在列表里
- 用 `df[条件].shape` 给筛选结果计数

**遇到的坑 & 怎么解决的**
- 问题：`df.info()` 显示 date 列的类型是 `str`（字符串），日期没法比较、没法按月统计
  - 解决：读取时加 `parse_dates=["date"]`，类型变为 `datetime64`，之后就能用 `.dt.month`、`.dt.day_name()` 等日期功能
- 问题：多条件筛选忘加圆括号会报错
  - 解决：记住规矩——`df[(条件1) & (条件2)]`，每个条件独立套一层圆括号

**产出**
- `read_demo.py`：读取 air_quality.csv，完成选列、筛选、计数 4 个练习（如筛出周末且 PM2.5>50 的天）

**明天计划**
- 学 `groupby` 分组统计：按星期几算平均 AQI

</details>

<details>
<summary><b>📖 07-28 周二 ｜ （示例：把当天主题写在这里）</b></summary>

**今天学了什么**
-

**遇到的坑 & 怎么解决的**
- 问题：
  - 解决：

**产出**
-

**明天计划**
-

</details>

**📌 本周小结**（周日填写）
- 本周关键词：
- 完成度自评（1~5 分）：
- 下周重点：

---

### 第 2 周（2026-07-20 ~ 07-26）—— Python 基础：变量、判断、循环、函数

<details>
<summary><b>📖 本周记录（当时未按天记，此处补充周总结）</b></summary>

**本周学了什么**
-

**遇到的坑 & 怎么解决的**
- 问题：
  - 解决：

**产出**
- `aqi_level.py`：基于 HJ 633—2026 标准的 AQI 分级程序（等级 + 颜色 + 健康建议 + 7 天统计）

**下周计划**
- Pandas 入门

</details>

---

### 第 1 周（2026-07-13 ~ 07-19）—— 环境搭建 + 建立 AI 辅助开发习惯

<details>
<summary><b>📖 本周记录（当时未按天记，此处补充周总结）</b></summary>

**本周学了什么**
- 搭建 Python + VSCode 环境，配置 Claude Code，完成第一次 Git 工作流

**遇到的坑 & 怎么解决的**
- 问题：
  - 解决：

**产出**
- GitHub 仓库 eco-ai-learning 建立并完成首次推送

**下周计划**
- Python 基础语法

</details>

---

## 📚 速查笔记

<details>
<summary><b>Pandas 第一课：读取和查看数据</b></summary>

| 指令 | 用处 |
|------|------|
| `import pandas as pd` | 导入 pandas，`pd` 是通用简称 |
| `pd.read_csv("air_quality.csv")` | 读取 CSV 文件，变成 DataFrame 表格 |
| `pd.read_csv("...", parse_dates=["date"])` | 读取时把 date 列识别为日期类型，不加会被当成普通字符串 |
| `df.head()` / `df.head(7)` | 看前 5 行 / 前 7 行 |
| `df.tail()` | 看最后 5 行，查最新数据 |
| `df.shape` | 行数和列数，如 `(7, 6)` |
| `df.columns` | 所有列名 |
| `df.info()` | 每列的类型 + 缺失值情况，数据"体检报告" |

</details>

<details>
<summary><b>Pandas 第二课：选列和筛选</b></summary>

| 指令 | 用处 |
|------|------|
| `df["aqi"]` | 选一列（得到 Series） |
| `df[["date", "pm25"]]` | 选多列，注意双层方括号 |
| `df["aqi"].mean()` / `.max()` | 算平均值 / 最大值 |
| `df[df["aqi"] > 100]` | 条件筛选：只留 AQI 超过 100 的行 |
| `df[(df["aqi"] > 50) & (df["aqi"] <= 100)]` | 多条件"且"，每个条件必须加圆括号 |
| `df[(df["aqi"] < 40) \| (df["aqi"] > 100)]` | 多条件"或" |
| `df["date"].dt.day_name()` | 从日期抽星期几（`.dt.month` 月份、`.dt.year` 年份） |
| `.isin(["Saturday", "Sunday"])` | 判断值是否在列表里（筛周末） |
| `df[df["aqi"] <= 50].shape` | 筛选结果计数 |

</details>

<details>
<summary><b>VSCode 技巧</b></summary>

- `Ctrl + /`：选中多行一键注释 / 取消注释
- `Ctrl + Z`：撤销；`Ctrl + Y`：重做
- `Ctrl + Shift + V`：打开 Markdown 预览
- `Ctrl + Shift + O`：按标题快速跳转（翻"目录"用）

</details>
